services:
  - type: web
    name: generate-report
    env: node
    buildCommand: npm install && npx playwright install chromium
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
